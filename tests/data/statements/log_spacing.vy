@internal
def foo(a: uint256):
    log      Event(
    a,            self.b,
          c*100+2-3,
          msg.sender      ,       total(a))
# output
@internal
def foo(a: uint256):
    log Event(a, self.b, c * 100 + 2 - 3, msg.sender, total(a))
